import Nav from "../Nav";
import SearchField from "../Search";
import CartIcon from "../CartIcon";

const Header = () => {
  return (
    <header>
      <Nav />
      <SearchField />
      <CartIcon />
    </header>
  );
};

export default Header;
